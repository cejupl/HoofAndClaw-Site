# Copy to .env and fill in. Never commit the real .env.

# Buttondown API key — used by `npm run notify` to email subscribers on a new post.
# Get it at https://buttondown.com/settings/api
BUTTONDOWN_API_KEY=

# Canonical site URL (used by the notify script for post links).
SITE_URL=https://hoofandclaw.game

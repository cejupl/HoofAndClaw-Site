# build output
dist/
.astro/
.vercel/

# deps
node_modules/

# env / secrets
.env
.env.*
!.env.example
# Cloudflare local dev secrets (wrangler / platformProxy)
.dev.vars
.wrangler/

# os / editor
.DS_Store
*.log

# Midjourney master art (kept locally; the curated, optimized copies in
# src/assets/{scenes,factions,gallery} + emblem.png are what the site uses).
src/assets/incoming/
